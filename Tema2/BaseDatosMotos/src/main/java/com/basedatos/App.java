package com.basedatos;

import com.basedatos.controller.MotoController;

public class App {
    public static void main(String[] args) {
        MotoController controller = new MotoController();
        controller.iniciarMenu();
    }
}
